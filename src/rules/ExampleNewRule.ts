class ExampleNewRule implements GameRule {
    execute(context: GameContext): void {
      // Implémentation de la règle
    }
  
    validate(context: GameContext): boolean {
      // Validation des conditions
      return true;
    }
  }